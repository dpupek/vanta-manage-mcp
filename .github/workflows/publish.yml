name: Publish to NPM
# Manual npm publish path only.
# This workflow is intentionally separate from main auto-tag release:
# - Main push auto-release uses `.github/workflows/release-main-tag.yml`
# - This workflow remains workflow_dispatch for registry publishing when needed.

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (leave empty for current package.json version)'
        required: false
        type: string

permissions:
  id-token: write  # Required for OIDC
  contents: read
  
jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Run linting and formatting checks
        run: |
          npm run lint
          npm run format:check
          
      - name: Build package
        run: npm run build
        
      - name: Update version if specified
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.version != ''
        env:
          VERSION: ${{ github.event.inputs.version }}
        run: npm version "$VERSION" --no-git-tag-version
        
      - name: Publish to NPM
        run: npm publish --access public
